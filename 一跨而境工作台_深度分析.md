# 一跨而境 AI 跨境电商 Listing 自动化工作台 · 深度分析

> 分析对象：`F:\yikuaborder-deploy`（包名 `chatgpt-listing-mvp`）
> 分析方法：直接读取编译后源码 `dist/src/*.js`、配置文件、提示词、运行态 JSON 与产品生命周期目录

---

## 0. 一句话结论

这是一个**本地运行的、以"浏览器自动化驱动真实 ChatGPT / Gemini / NotebookLM 网页"为核心的跨境电商 Listing 生产工作台**。它把"上传产品图 → 联网市场调研 → 视觉规划 → AI 生图 → SEO / Listing 文案 → 归档"整条链路做成一个**可断点续跑的状态机**，并为奢侈包内胆（insert）额外提供"市场雷达选款 + NotebookLM 库存匹配 + 设计冻结"的独立流水线。后端是 Express，前端是单页应用，所有持久化都用本地 JSON / JSONL 文件。

**关键识别点**：项目**不直接调用任何 LLM 官方 API**，全部通过 Playwright 的 CDP 连接本机专用 Chrome，操作 `chatgpt.com` / `gemini.google.com` / `notebooklm.google.com` 的真实网页会话来完成调研、生图与文案。这规避了 API 配额/风控，也复用了真人账号的会话上下文。

---

## 1. 项目基本信息

| 项 | 值 |
|---|---|
| 包名 / 描述 | `chatgpt-listing-mvp` · "一跨而境 AI 跨境电商视觉与 Listing 自动化工作台" |
| 技术栈 | Node + TypeScript（编译到 `dist/`）、Express、`multer`（上传）、`playwright-core`（浏览器自动化）、`ajv`（档案校验）、`jsonrepair`、`xlsx`（队列导入） |
| 入口 | `node dist/src/server.js`，默认监听 `127.0.0.1:3000`（`HOST` / `PORT` 可改） |
| 运行模式 | 单机/小团队本地运行；`USER_DATA_DIR` 环境变量支持多用户数据隔离 |
| 核心特征 | 浏览器自动化驱动真实网页会话；状态机 + 断点续跑；本地文件原子写入 |

---

## 2. 目录结构与组织方式

项目按"**代码 / 资源 / 数据 / 生命周期产物**"四类清晰分层：

| 路径 | 作用 | 关键内容 |
|---|---|---|
| `dist/src/` | 编译后的后端服务（24 个 ES Module） | `server.js`(2350行) `luxury-insert-service.js`(2045) `automation-service.js`(1004) `operation-service.js` `queue-service.js` 等 |
| `dist.old/` | 上一版编译产物（回滚用） | 同结构旧版 |
| `public/` | 前端单页应用 | `index.html` · `app.js`(425KB) · `styles.css` + `p0-visualization.html` / `smt-choice-task-card-preview.html` 两个预览页 |
| `prompts/` | 7+ 个 LLM 提示词模板 | `01_RESEARCH_PROMPT`（市场调研）、`02_VISUAL_PLANNING`（视觉规划）、`03_SEO_KEYWORDS`、`04_LISTING_CONTENT`、`05_LUXURY_INSERT`、`06_INSERT_MARKET_RADAR`、`07_INSERT_LISTING_CONTENT`（另有 v2 系列内联于 config） |
| `schemas/` | 数据契约 | `product-profile.schema.json`（商品档案 JSON Schema，Ajv2020 校验） |
| `当前产品/` | **进行中**商品的工作区 | `产品图/` `output/` `目标外包图/` `内胆图/` `内胆output/` + `product-profile.json` |
| `已完成产品/` | 已归档成品（按"品牌 包型"建子目录） | 内图/输出图/各阶段 md / `product-profile.json` / `run-state.json` |
| `已遗弃产品/` | 主动放弃的商品 | 按时间戳建子目录 |
| `商品队列/` | 批量队列 | `queue-state.json` + `tasks/<taskId>/`（每任务含 `task.json` 与 `产品图/`） |
| `data/` | "隐藏数据环"与运行辅助 | `command_inbox/event_log/qclaw_*/action_log/snapshots/reviews/knowledge_items` 等 JSONL、`product-visual-assets.json`、`today_dashboard.json`、`qclaw_state.json` |
| 根级 JSON | 运营/队列/档案状态 | `run-state.json`（全局状态机）、`operation-*.json`、`store-profiles.json`、`listing-cards.json`、`listing-sku-mappings.json`、`performance-snapshots.json`、`operation-actions.json`、`operation-rules.json`、`daily-waiting-items.json`、`operators.json` |
| 启动脚本 | 多平台启动器 | `start-windows.bat` `start-unix.sh` `_run_lance.ps1` `start-lance.bat` `start-laodu.bat` |

**组织规律**：所有"正在处理"的数据活在 `当前产品/` 与 `run-state.json`；一旦完成或放弃，整目录被搬移到 `已完成产品/` 或 `已遗弃产品/`，实现"单一活动工作区 + 归档库"的生命周期管理。

---

## 3. 核心模块职责

### 3.1 入口与装配
- **`server.js`**（2350 行）：Express 路由总入口 + 服务装配中心。启动时 `new` 出全部 service 并互相注入依赖；所有长任务经 `runBackground` + `sleepInhibitor` 异步跑、路由立即返回 202。约 100+ 路由，按功能分组（见 §4.3）。
- **`config.js`**（726 行）：集中所有路径常量、提示词正文、状态机阶段常量、浏览器/适配器配置。仅读 5 个环境变量（`USER_DATA_DIR`/`PORT`/`HOST`/`CHROME_DEBUG_PORT`），其余为硬编码默认值；内联了全部 v2 提示词与故事板闸门提示词。

### 3.2 状态与编排核心（蓝色层）
- **`state-store.js`**：全局单例状态机，读写 `run-state.json`；启动时崩溃恢复（running→PAUSED）。
- **`automation-service.js`**：**标准 Listing 编排器**。状态机驱动单品从建对话→事实提取→调研→规划→生图→SEO 文案，支持 `runAll` 自动串联与断点 `resume`。
- **`luxury-insert-service.js`**（2045 行）：**奢侈内胆编排器**。另一条更重的流水线，含市场雷达、识别包型、NotebookLM 库存匹配、设计冻结、7 张 1:1 内胆图、库存尺寸表回写。
- **`queue-service.js`**：xlsx 批量队列。`importWorkbook` 把每行变一个任务并下载图片，`runLoop` 逐任务跑完→归档→下一个。
- **`product-profile-service.js`**：每个商品的档案（Ajv2020 校验），作为 state 的**观察者**自动生成；含当前档案 + 已归档库（轻量建档/老品补建档）。
- **`product-session.js`**：归档/遗弃时目录搬移工具（复制到 `已完成产品/<中文名>` 或 `已遗弃产品/`）。

### 3.3 AI 适配器层（浏览器自动化）
- **`ai-adapter.js`**：空索引文件（`export {}`），仅占位；真正的抽象在 `server.js` 的 `adapters = { chatgpt, gemini }`。
- **`chatgpt-adapter.js` / `gemini-adapter.js`**：通过 `chromium.connectOverCDP(127.0.0.1:9223)` 操作对应网页——建对话、上传图、开网页搜索/图片创建、轮询等回复、从 `<img>` 抓高清图 base64 下载；含"真人验证/未登录"识别。
- **`notebooklm-adapter.js`**：同一 Chrome 操作 NotebookLM 查询框，用于内胆库存匹配（不生图）。

### 3.4 支撑服务
- **`operation-service.js`**（运营中台）：商品运营档案、店铺、Listing 卡、SKU 映射、周期性能快照（算 ctr/cvr/roi/退款率 + 规则建议）、运营动作审核状态机。
- **`daily-operation-service.js`**：每日"等待事项"台账（owner/到期/跟进时间）。
- **`operator-service.js`**：运营人员名册（当前 `lance` / `anna` / `duxiqiu`）。
- **`hidden-data-loop-service.js`**（隐藏数据环）：命令/事件/动作/QClaw 任务的 JSONL 追加与读取、QClaw 状态机、当日看板重建。
- **`storyboard-gate-service.js`**：生图前"故事板闸门"校验（必须 10 图、角色/卖点/视觉证据不重复、Prompt Pack 与故事板一致、禁止拼版/宫格）。
- **`p0-protocol-service.js`**：仅读取 `test/fixtures/p0/` 静态夹具，作协议校验演示，无真实业务逻辑。
- **`product-visual-assets-service.js`**：参考图资产库，sha256 去重 + 生图前校验。
- **`directory-images.js` / `image-files.js`**：图片导入/扫描/URL 下载（**防 SSRF** `assertPublicHost`）/删除。
- **`prompt-files.js`**：提示词读取/保存/校验（Prompt Pack 边界 `<<<IMAGE_NN_PROMPT_...>>>`）。
- **`json-write-queue.js`**：单文件串行写队列，避免并发写坏。
- **`sleep-inhibitor.js`**：macOS `caffeinate` 防睡眠（Windows 仅打印错误，不阻塞）。

---

## 4. 模块依赖与调用流程

### 4.1 装配依赖（server.js 启动时）
```
StateStore(store)
  ├─ adapters = { chatgpt: ChatGptAdapter, gemini: GeminiAdapter }
  ├─ ProductVisualAssetsService(visualAssets)
  ├─ AutomationService(store, adapters, visualAssets)        // 标准 Listing
  ├─ LuxuryInsertService(store, adapters, new NotebookLmAdapter())  // 奢侈内胆
  ├─ SleepInhibitor
  ├─ ProductProfileService(productProfiles)   // store.setUpdateObserver → syncFromState
  ├─ QueueService(store, standardService, productProfiles)
  └─ OperationService / DailyOperationService / OperatorService
      / HiddenDataLoopService / P0ProtocolService
```
启动顺序：`mkdir` 各目录 → `productProfiles/operation/daily/operator/hiddenData` 初始化 → `store.load()` → 注册观察者 → `queueService.load()`。

### 4.2 文件级 import 关系（精简）
- `server.js` 依赖几乎全部模块（装配点）。
- `automation-service` → `config` `image-files` `prompt-files` `product-visual-assets-service`。
- `luxury-insert-service` → `config` `jsonrepair` `playwright-core` `directory-images` `prompt-files` `product-session` `image-files`。
- `queue-service` → `config` `xlsx` `directory-images` `product-session` `product-profile-service`。
- `product-profile-service` → `config` `product-session`（Ajv2020）。
- `state-store` → `config` `storyboard-gate-service`（`normalizeGenerationQcResult`）。
- 三个 adapter → `config` `playwright-core`。

### 4.3 端到端调用流程
**标准单品**：`POST /api/run` → `runBackground(standardService.start)` → 浏览器建 ChatGPT/Gemini 对话 → 上传产品图 → 产品事实提取(MVP1) → `run/all` 或分步：`run/planning`(调研→规划 Prompt Pack, MVP3) → `run/images`(逐张生图 MVP4) → `run/seo-listing`(SEO/Listing 文案 MVP5) → `POST /api/product/next` 归档 + `productProfiles.archiveCurrent`。

**批量队列**：`POST /api/queue/import`(.xlsx) → 每任务下载图片到 `tasks/<id>/产品图` → `POST /api/queue/start` → `runLoop` 逐任务 `loadTask`(拷图入当前产品) → `automation.runAll()` → `archiveCurrentProduct` + `profiles.archiveCurrent` → 取下一个，直到 `completed`。

**奢侈内胆**：`PUT /api/workflow-mode=luxury_insert` → `insert/run/market-radar`(选款) → `insert/run/identify`(识别包型) → `insert/bag-confirmation`(人工确认) → `insert/run/notebook`(NotebookLM 库存匹配) → `insert/design-freeze`(人工冻结) → 上传内胆图 → `insert/run/prompts` → `insert/run/images`(7 图) → `insert/run/listing-content` → 写库存表 → `insert/archive`。

### 4.4 状态机阶段（`product-profile.schema.json` 的 `lifecycle.currentStage` 枚举）
标准流：`IDLE → VALIDATING_INPUT → CREATING_CHAT → UPLOADING_IMAGES → SENDING_PROMPT → WAITING_FOR_RESPONSE → SENDING_RESEARCH → WAITING_FOR_RESEARCH → SENDING_PLANNING → WAITING_FOR_PLANNING → VALIDATING_PROMPT_PACK → GENERATING_IMAGES → DOWNLOADING_IMAGE → SENDING_SEO_KEYWORDS → … → SAVING_LISTING_CONTENT → COMPLETED`，外加 `PAUSED` / `FAILED`。
奢侈流在尾部叠加：`INSERT_MARKET_RADAR → INSERT_IDENTIFYING_BAG → INSERT_WAITING_BAG_CONFIRMATION → INSERT_QUERYING_NOTEBOOK → INSERT_WAITING_DESIGN_FREEZE → INSERT_WAITING_LINER_IMAGES → INSERT_BUILDING_PROMPTS → INSERT_GENERATING_IMAGES → INSERT_ARCHIVING`。
每个阶段都有 `WAITING_*` 配对态，体现"发送 Prompt → 轮询等待网页回复"的浏览器自动化节奏，使断点续跑（resume）成为可能。

---

## 5. 数据模型与持久化

| 文件 | 职责 |
|---|---|
| `run-state.json` | 全局状态机（stage/provider/workflowMode/luxuryInsert/error 等），`state-store` 读写 |
| `当前产品/product-profile.json` | 当前活动商品的档案（Ajv 校验）；含 `identity` / `lifecycle` / `nextAction` / `artifacts` / `archive` / `queueSource` |
| `商品队列/queue-state.json` + `tasks/<id>/task.json` | 队列与单个任务状态（`preparing/ready/running/paused/completed/abandoned/cancelled`） |
| `operation-profiles.json` / `store-profiles.json` / `listing-cards.json` / `listing-sku-mappings.json` / `performance-snapshots.json` / `operation-actions.json` | 运营中台各实体 |
| `operation-rules.json` | 自动判定规则（如 CTR<1%、ROI<1 触发建议动作） |
| `data-collection-tasks.json` | 数据采集任务 |
| `daily-waiting-items.json` | 每日待办台账 |
| `operators.json` | 运营人员名册 |
| `data/*.jsonl` | 隐藏数据环（命令/事件/动作/QClaw/快照/评测/知识条目）+ `today_dashboard.json` |
| `data/product-visual-assets.json` | 参考图资产库（sha256 去重重用） |
| `schemas/product-profile.schema.json` | 商品档案契约（校验门） |

**写入策略**：所有写操作经 `json-write-queue` 串行化 + 临时文件原子 `rename`，防止并发损坏。

---

## 6. 两条业务流水线对比

| 维度 | 标准 Listing (`standard_listing`) | 奢侈内胆 (`luxury_insert`) |
|---|---|---|
| 目标 | 普通跨境商品的上架主图 + Listing | 奢侈包（如 Goyard）的内胆/收纳包 insert |
| 生图 | 10 张 3:4 | 7 张 1:1（内胆图） |
| 联网调研 | 有（市场 Top10 视觉审计） | 有（更重的"市场雷达"选款 + 包型识别） |
| NotebookLM | 否 | 是（内胆现货尺寸表库存匹配） |
| 人工闸门 | nextAction 建议/覆盖 | 包型确认 + 设计冻结（强闸门） |
| 库存表回写 | 否 | 是（Google Sheets webhook） |
| 归档位置 | `已完成产品/` | `已完成产品/<品牌 包型 内胆>/` |

标准流还有 `standardWorkflowGoal` 子模式：`full_listing`（全量）与 `seo_content_only`（只出 SEO/文案不出图）。

---

## 7. 应用场景与主要功能

### 应用场景
面向**跨境卖家 / 运营小团队**，为 Amazon、eBay、SHEIN、Walmart、Shopify 等平台上架商品时，需要**批量、合规、高转化**的主图与 Listing 文案，但缺乏设计/文案人力。本工作台用 AI 自动化完成从"一张产品实拍图"到"一套可上架素材"的生产。

### 主要功能（映射到端点分组）
1. **产品建档与图片导入**：`/api/import-images`、商品档案 CRUD、轻量/老品补建档。
2. **标准 Listing 生成**：调研、视觉规划、10 图生图、SEO/Listing 文案一键 `run/all` 或分步触发。
3. **奢侈包内胆生成**：市场雷达选款、包型识别、NotebookLM 库存匹配、设计冻结、7 图生图、库存表回写。
4. **批量队列**：xlsx 导入 → 自动逐任务跑完 → 归档。
5. **运营中台**：商品/店铺/Listing/SKU 映射/性能快照/运营动作，含 CTR/ROI/CVR 规则建议。
6. **每日待办**：等待事项台账与跟进。
7. **隐藏数据环 / 命令中枢**：命令收件箱、事件/动作日志、QClaw 任务状态机、飞书 webhook 接收、当日看板。
8. **多智能体任务注入**：`/api/agent/*` 让 workbuddy/codex 等外部 agent 投递/认领/回报任务。
9. **状态与监控**：`/api/status` 聚合状态、队列、运营、档案、资产、图片清单；浏览器/引擎/工作流模式切换。

---

## 8. 关键设计特征与工程取舍

- **浏览器自动化而非 API**：复用真人网页会话，规避配额/风控，但强依赖本机专用 Chrome（CDP 9223）与登录态，断连即 PAUSED。
- **状态机 + 断点续跑**：每个阶段都有 `WAITING_*` 配对态，支持 `pause` / `resume` / `rollback` / `abandon`。
- **本地 JSON 原子写入**：`json-write-queue` + 临时文件 rename，防并发损坏；无数据库。
- **防 SSRF**：图片 URL 下载经 `assertPublicHost`，拒绝 localhost/内网。
- **人工闸门**：设计冻结、包型确认、`nextAction.manualOverride`，把不可自动确认的事实留给人工。
- **多工作流模式**：`standard_listing` / `luxury_insert` + `standardWorkflowGoal` 子模式，单状态机承载双流水线。
- **单人/小团队定位**：本地 `127.0.0.1`、无鉴权、无多租户（仅 `USER_DATA_DIR` 隔离），`sleep-inhibitor` 主力在 macOS。

---

## 9. 小结

这是一个**高度面向真实业务闭环、以浏览器自动化为技术内核**的跨境电商 Listing 生产系统：它把"调研—规划—生图—文案—归档"做成状态机，并针对不同品类（普通商品 / 奢侈包内胆）提供两条可调用的流水线，外围辅以运营中台、每日待办、隐藏数据环与多智能体注入。整体工程上偏"单人纵向打深"，所有产物本地文件化、可断点续跑、可人工干预，是一个典型的"AI 原生工作流工具"而非通用 SaaS。

**可观察的扩展点**：P0 协议（目前仅为静态夹具）、飞书（目前仅收命令）、多平台店铺 Listing 下发、运营规则引擎自动化——这些都已预留接口，是后续横向扩展的自然方向。
