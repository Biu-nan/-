# 乳贴 · 店小秘必填字段规范 v1

> 生成时间：2026-07-21
> 来源：店小秘 Choice 半托管 / 乳贴创建页（引用源产品 `1005005575013300` 后的 `quoteEdit` 草稿，URL `https://www.dianxiaomi.com/web/smtChoice/quoteEdit?id=114841450023054570`）
> 用途：作为「引用模式上品」唯一字段真相源 —— 决定哪些字段由引用带入、哪些从 `product-facts.json` 覆盖、哪些需人工补全。

---

## 一、字段总览（去重后 37 个，必填 9 个）

### A 区 · 必填字段（保存前必须填，否则校验失败）

| # | 字段 | rc_select / id | 类型 | 引用带入值 | 数据来源 | 现状 |
|---|------|---------------|------|-----------|---------|------|
| 1 | 店铺名称 | `rc_select_0` | select | 空 | 固定常量 `洛慈全托管` | ✅ fillFixedFields 已填 |
| 2 | 备货类型 | `rc_select_1` | select | 空 | 固定常量 `国内履约` | ✅ fillFixedFields 已填 |
| 3 | 产品标题 | `input`(无 id) | input | `1 Pair Sexy Metal…`(源产品) | facts.title | ⚠️ fillFromFacts 已覆盖，但当前 facts 是源产品旧值 |
| 4 | 产品分类 | `rc_select_2` | select | 内衣配件（乳贴/乳垫/肩带等） | 引用带入 | ✅ 同品类无需改 |
| 5 | **产品图片** | `productMainImgModule` | upload | **空** | facts.images.main | 🔴 **缺失**：facts.images.main=[] 空数组 |
| 6 | **尺寸(Size)** | `input`(无 id) | input | 空 | 销售属性/事实 | 🔴 **缺失**：SKU 销售属性维度，未填 |
| 7 | **PC端描述** | `textarea`(无 id) | textarea | 有 HTML(源产品) | facts.description.pc | ⚠️ fillFromFacts 已覆盖，内容待优化 |
| 8 | 计件单位 | `form_item_productUnit` | select | 空 | 固定/事实 | 🟡 未填（通常 `set`/双/对） |
| 9 | 海关监管属性 | `input`(无 id) | input | `3` | 固定默认 `3` | 🟡 默认值已带，一般不动 |

### B 区 · 选填类目属性（引用带入源产品值，需按新 facts 覆盖）

| # | 字段 | rc_select | 引用带入值 | 应从 facts 取 | 现状 |
|---|------|-----------|-----------|--------------|------|
| 10 | 产品属性(模板) | `rc_select_11` | ccbodily | 模板同步 | ✅ 引用带入，保留 |
| 11 | **品牌(Brand Name)** | `rc_select_11` | 同产品属性(同 id!) | facts.brand | 🔴 **边界**：无独立输入框，品牌随模板派生，落不了 RUTIE |
| 12 | 高关注化学品 | `rc_select_12` | 空 | facts/默认 `天然未处理` | 🔴 fillFromFacts 未触达 |
| 13 | 型号(Model Number) | `input` | `NPE0005`(源产品) | facts.model | 🔴 源产品旧值，需覆盖 |
| 14 | 产地（国家或地区） | `rc_select_13` | 空 | facts.origin.country | 🔴 fillFromFacts 未触达 |
| 15 | 中国省份 | `rc_select_22` | 空 | facts.origin.province | 🔴 fillFromFacts 未触达 |
| 16 | 是否性暗示 | `rc_select_14` | 空 | 默认 `否(No)` | 🔴 fillFromFacts 未触达 |
| 17 | 是否暴露图片 | `rc_select_15` | 空 | 默认 `否(No)` | 🔴 fillFromFacts 未触达 |
| 18 | 颜色风格 | `rc_select_16` | 空 | facts.keyAttributes.颜色 | 🔴 fillFromFacts 未触达 |
| 19 | 内衣配件类型 | `rc_select_17` | `Breast Petals`(源产品) | facts/默认 | 🔴 源产品旧值，需覆盖 |
| 20 | 材质(Material) | `rc_select_18` | 空 | facts.material | ✅ fillFromFacts 已填（但当前 facts 材质为「金属链条」需改） |
| 21 | 产品类型 | `rc_select_19` | 空 | facts.keyAttributes.造型 | 🔴 fillFromFacts 未触达 |
| 22 | 性别 | `rc_select_20` | 空 | 默认 `女性(Women)` | 🔴 fillFromFacts 未触达 |
| 23 | 编织工艺 | `rc_select_21` | 空 | facts/默认 | 🔴 fillFromFacts 未触达 |
| 24 | 自定义属性 | — | 空 | 可选 | ⚪ 一般不动 |

### C 区 · 销售属性 / SKU（变种）

| # | 字段 | id | 类型 | 说明 | 现状 |
|---|------|----|------|------|------|
| 25 | 颜色(Color) | `input`(无 id) | input | 销售属性维度① | 🔴 未配置具体值 |
| 26 | 尺码表 | `rc_select_4` | select | 尺码模板 | ⚪ 均码可留空 |
| — | **SKU 组合表** | 动态 table | table | 点「生成SKU」后按 颜色×尺寸 生成，每行含 价格/库存/条码/重量/尺寸 | 🔴 **完全未处理**（Step 4） |

> 注意：当前草稿已存在 `颜色(Color)` + `尺寸(Size)` 销售属性输入框，但尚未点「生成SKU」，故无 SKU 组合表。这是 Step 4 的核心。

### D 区 · 选填（一般不动，或按店铺策略）

营销图片、产品视频、发货地、欧盟责任人、土耳其责任人、品牌制造商、资质信息、商机品关联、店小秘分类、来源URL、无线端描述、不填写无线端描述

---

## 二、数据源决策（引用 vs facts vs 固定）

```
引用自动带入（保留，不覆盖）
  ├─ 产品分类 rc_select_2
  ├─ 产品属性模板 rc_select_11 (ccbodily)
  └─ 品牌骨架（随模板派生）

固定常量（fillFixedFields）
  ├─ 店铺名称 rc_select_0 = 洛慈全托管
  └─ 备货类型 rc_select_1 = 国内履约

从 product-facts.json 覆盖（fillFromFacts 扩展）
  ├─ 产品标题        ← facts.title
  ├─ PC端描述         ← facts.description.pc
  ├─ 无线端描述       ← facts.description.mobile
  ├─ 材质 rc_select_18 ← facts.material
  ├─ 高关注化学品/产地/性暗示/暴露图/颜色风格/内衣配件类型/产品类型/性别/编织工艺 ← facts 对应键
  ├─ 型号             ← facts.model
  ├─ 产品图片         ← facts.images.main[]
  └─ SKU 组合         ← facts.variants.skus[]  (Step 4)

人工/默认（代码给合理默认，可人工改）
  ├─ 计件单位         → 默认 set/双
  ├─ 海关监管属性      → 默认 3
  ├─ 是否性暗示/暴露图 → 默认 否(No)
  ├─ 性别             → 默认 女性(Women)
  └─ 品牌             → 边界：落不了自定义值（已知限制）
```

---

## 三、对应你反馈的 5 大问题 → 根因

| 你的反馈 | 根因（本规范定位） | 归属步骤 |
|---------|------------------|---------|
| ① 信息填写不完整 | B 区 11 个类目属性 fillFromFacts 未触达 + 品牌边界 | Step 3 |
| ② 没有上传主图 | A5 必填但 `facts.images.main=[]` 空 | Step 2 |
| ③ 只做 PC/无线详情页 | A7/A(无线) 已填，但内容为源产品旧 HTML，且无线端描述本质是「不填写无线端描述」的同级项 | Step 3 内容优化 |
| ④ 没有设置变种 SKU | C 区 SKU 组合表完全未生成/填写 | Step 4 |
| ⑤ 属性信息不完整 | 同①，引用带入的值是源产品的，未按新 facts 覆盖 | Step 3 |

---

## 四、后续执行步骤（已建任务 #24–#27）

- **Step 2**（#25）：修复主图上传链路 — 上传成功后把文件名写回 `product-facts.json.images.main[]`，让 `uploadProductImages` 有真实文件。
- **Step 3**（#26）：扩展 `fillFromFacts`，把 B 区类目属性 + 型号 + 计件单位按 facts/默认覆盖。
- **Step 4**（#27）：调研 SKU 组合表 DOM，从 `facts.variants.skus[]` 填价格/库存/SKU编码。
- **品牌**：记录为已知边界（乳贴品类无独立 brand 输入），待确认店小秘是否有后台入口。

---

## 附录：机器可读规范

同目录 `乳贴必填字段规范_v1.json`（由 `audit-fields.mjs` 扫描生成，含每个字段的 `label/required/type/rcId/current`）。
